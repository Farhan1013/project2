<%= form_for( @post, :url => {:action => :chatter_post, :controller => :post} ) do |f| %>

  <!--For now we have simplified this form by removing all the built in error handling,   
  in reality when we are bosses well probably handle the display of the errors with some
  fancy javascript or angular, but for now we keep things simple and clear -->

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>